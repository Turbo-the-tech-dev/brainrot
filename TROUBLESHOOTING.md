# Troubleshooting Guide

## Installation Errors
- Ensure you are on Node.js v18 or higher.
- Try deleting `node_modules` and running `npm install` again.

## Environment Variables
- Make sure you have created a `.env` file based on `.env.example`.
